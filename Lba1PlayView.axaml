<UserControl x:Class="LBAAssembler.Lba1PlayView"
        xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="#FFFFFF" Foreground="#10243E" FontFamily="{StaticResource EditorFont}" Focusable="True"
       >
    <UserControl.Styles>
        <Style Selector="Button">
            <Setter Property="Foreground" Value="#10243E"/>
            <Setter Property="Background" Value="#D6E6F7"/>
            <Setter Property="BorderBrush" Value="#A9C3E0"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Focusable" Value="False"/>
        </Style>
        <Style Selector="CheckBox">
            <Setter Property="Foreground" Value="#10243E"/>
            <Setter Property="Margin" Value="0,0,14,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Focusable" Value="False"/>
        </Style>
        <Style Selector="ComboBox">
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Focusable" Value="False"/>
        </Style>
    </UserControl.Styles>
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#D6E6F7" BorderBrush="#A9C3E0" BorderThickness="0,0,0,1" Padding="12,8">
            <WrapPanel Orientation="Horizontal">
                <TextBlock Text="Scene" Foreground="#4E6B8A" FontSize="10" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="SceneCombo" Width="330" SelectionChanged="SceneCombo_SelectionChanged"/>
                <Button x:Name="RestartButton" Content="Restart scene" Click="Restart_Click"/>
                <Button x:Name="PauseButton" Content="Pause" Click="Pause_Click"/>
                <Button x:Name="LoadoutButton" Content="Twinsen's state…" Click="Loadout_Click"/>
                <Button x:Name="FilmsButton" Content="Films…" Click="Films_Click"/>
                <TextBlock Text="Twinsen" Foreground="#4E6B8A" FontSize="10" VerticalAlignment="Center" Margin="6,0,8,0"/>
                <ComboBox x:Name="BehaviourCombo" Width="110" SelectionChanged="Behaviour_SelectionChanged">
                    <ComboBoxItem Content="Normal" IsSelected="True"/>
                    <ComboBoxItem Content="Sporty"/>
                    <ComboBoxItem Content="Aggressive"/>
                    <ComboBoxItem Content="Discreet"/>
                </ComboBox>
                <CheckBox x:Name="ZonesCheck" Content="Zones" IsChecked="True" Click="Zones_Click"/>
                <CheckBox x:Name="FollowCheck" Content="Follow Twinsen" IsChecked="True"/>
                <CheckBox x:Name="GameViewCheck" Content="Game camera (640×480)" IsChecked="False" IsCheckedChanged="GameView_Click"/>
                <CheckBox x:Name="ClickCheck" Content="Click moves Twinsen" IsChecked="False"/>
                <CheckBox x:Name="SoundCheck" Content="Sound" IsChecked="True"/>
                <CheckBox x:Name="MusicCheck" Content="Music" IsChecked="True" Click="Music_Click"/>
                <CheckBox x:Name="ShadowsCheck" Content="Shadows" IsChecked="True"/>
                <CheckBox x:Name="BoxesCheck" Content="Boxes" IsChecked="False"/>
                <CheckBox x:Name="LabelsCheck" Content="Numbers" IsChecked="True"/>
                <TextBlock Text="Zoom" Foreground="#4E6B8A" FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <Slider x:Name="ZoomSlider" Minimum="0.4" Maximum="2.5" Value="1" Width="110" VerticalAlignment="Center" Focusable="False" ValueChanged="Zoom_ValueChanged"/>
                <CheckBox x:Name="MuteAllCheck" Content="Mute all" Click="MuteAll_Click" ToolTip.Tip="Silence music, speech and effects (the balance is saved per game)"/>
                <TextBlock Text="Music" Foreground="#4E6B8A" FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <Slider x:Name="MusicSlider" Minimum="0" Maximum="100" Value="40" Width="70" VerticalAlignment="Center" Focusable="False" Margin="0,0,10,0" ValueChanged="AudioSlider_Changed"/>
                <TextBlock Text="Voices" Foreground="#4E6B8A" FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <Slider x:Name="VoicesSlider" Minimum="0" Maximum="100" Value="90" Width="70" VerticalAlignment="Center" Focusable="False" Margin="0,0,10,0" ValueChanged="AudioSlider_Changed"/>
                <TextBlock Text="Effects" Foreground="#4E6B8A" FontSize="10" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <Slider x:Name="EffectsSlider" Minimum="0" Maximum="100" Value="70" Width="70" VerticalAlignment="Center" Focusable="False" Margin="0,0,10,0" ValueChanged="AudioSlider_Changed"/>
            </WrapPanel>
        </Border>
        <Border DockPanel.Dock="Right" Width="330" Background="#E8F0FA" BorderBrush="#A9C3E0" BorderThickness="1,0,0,0" Padding="12,10">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" x:Name="StatusText" FontFamily="{StaticResource EditorMonoFont}" FontSize="11" Foreground="#10243E" TextWrapping="Wrap"/>
                <TextBlock DockPanel.Dock="Top" Text="Arrows walk and turn   Space action   F1-F4 behaviour   P pause" TextWrapping="Wrap" FontSize="10" Foreground="#4E6B8A" Margin="0,8,0,8"/>
                <TextBlock DockPanel.Dock="Top" Text="What the scripts did" FontSize="10" Foreground="#4E6B8A" Margin="0,0,0,4"/>
                <ListBox x:Name="EventList" Background="#FFFFFF" BorderBrush="#A9C3E0" Foreground="#10243E" FontFamily="{StaticResource EditorMonoFont}" FontSize="10.5" Focusable="False"/>
            </DockPanel>
        </Border>
        <Grid>
            <ScrollViewer x:Name="MapScroll" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="#FFFFFF" Focusable="False">
                <LayoutTransformControl x:Name="MapScaleHost" HorizontalAlignment="Left" VerticalAlignment="Top">
                <LayoutTransformControl.LayoutTransform><ScaleTransform ScaleX="1" ScaleY="1"/></LayoutTransformControl.LayoutTransform>
                <Grid x:Name="MapHost" PointerPressed="Map_MouseLeftButtonDown" Background="#FFFFFF">
                    <Image x:Name="MapImage" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <Canvas x:Name="ZoneCanvas" IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    <Canvas x:Name="OverlayCanvas" IsHitTestVisible="False" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                </Grid>
                </LayoutTransformControl>
            </ScrollViewer>
            <!-- a film (FLA from the CD) takes the whole view; Esc skips it -->
            <Border x:Name="FilmBox" IsVisible="False" Background="Black" ZIndex="20">
                <Grid>
                    <Image x:Name="FilmImage" Stretch="Uniform" RenderOptions.BitmapInterpolationMode="LowQuality" Margin="10"/>
                    <TextBlock x:Name="FilmHint" Text="ESC skips" Foreground="#4E6B8A" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,14,10"/>
                </Grid>
            </Border>
            <!-- the game's status: life, magic, money, keys, clover -->
            <Border x:Name="Hud" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,10" Padding="10,7" CornerRadius="5"
                    Background="#CCF3F8FF" BorderBrush="#A9C3E0" BorderThickness="1" IsHitTestVisible="False">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="♥" Foreground="#E05555" FontSize="14" Width="20"/>
                        <ProgressBar x:Name="LifeBar" Width="150" Height="10" Maximum="50" Foreground="#D84545" Background="#2A1A1A" BorderThickness="0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0" x:Name="MagicRow">
                        <TextBlock Text="✦" Foreground="#5B9BEB" FontSize="14" Width="20"/>
                        <ProgressBar x:Name="MagicBar" Width="150" Height="7" Maximum="80" Foreground="#4A8AE0" Background="#1A2233" BorderThickness="0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock x:Name="HudText" Foreground="#10243E" FontFamily="{StaticResource EditorMonoFont}" FontSize="12" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
            <!-- Shift: the inventory, which stops the clock like the game's -->
            <Border x:Name="InventoryBox" IsVisible="False" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="18,14" CornerRadius="6"
                    Background="#F0F3F8FF" BorderBrush="#4E6B8A" BorderThickness="2" IsHitTestVisible="False" Width="620">
                <StackPanel>
                    <TextBlock Text="Inventory" FontSize="10" Foreground="#4E6B8A" Margin="0,0,0,6"/>
                    <UniformGrid x:Name="InventoryGrid" Columns="4"/>
                    <TextBlock x:Name="InventoryDesc" Foreground="#10243E" FontSize="13" TextWrapping="Wrap" Margin="0,10,0,0" MinHeight="70"/>
                    <TextBlock Text="Arrows choose   Enter use   Esc or Shift close" FontSize="10" Foreground="#4E6B8A" Margin="0,6,0,0" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
            <!-- what the scripts say: the game's text box, over the map while it is open (it stops the clock) -->
            <Border x:Name="DialogueBox" IsVisible="False" VerticalAlignment="Bottom" Margin="40,0,40,28" Padding="22,16"
                    Background="#EEF3F8FF" BorderBrush="#4E6B8A" BorderThickness="2" CornerRadius="6" IsHitTestVisible="False">
                <StackPanel>
                    <TextBlock x:Name="DialogueSpeaker" FontSize="10" Foreground="#4E6B8A" Margin="0,0,0,6"/>
                    <TextBlock x:Name="DialogueText" FontSize="17" TextWrapping="Wrap" Foreground="#10243E"/>
                    <StackPanel x:Name="DialogueChoices" Margin="0,10,0,0"/>
                    <TextBlock x:Name="DialogueHint" FontSize="10" Foreground="#4E6B8A" Margin="0,10,0,0" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
